services:
  server:
    container_name: python-scraper
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/usr/src/app
    command: ["gunicorn", "app:app", "-b", "0.0.0.0:8000"]
    environment:
      - REMOTE_DRIVER=https://standalone-firefox-latest.onrender.com:4444
